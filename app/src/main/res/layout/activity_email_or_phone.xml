<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".EmailOrPhone">

    <!-- Recovery Method Prompt -->
    <TextView
        android:id="@+id/textViewPrompt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="How would you like to recover?"
        android:textSize="24sp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"
        android:textAlignment="center"/>

    <!-- Email Button -->
    <Button
        android:id="@+id/email"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textViewPrompt"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@id/centerGuide"
        android:layout_marginTop="50dp"
        android:layout_marginEnd="8dp"
        android:background="@drawable/buttoncolor"
        android:text="Email"/>

    <!-- Invisible center guide to help align buttons -->
    <View
        android:id="@+id/centerGuide"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_centerHorizontal="true"/>

    <!-- Phone Button -->
    <Button
        android:id="@+id/phone"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textViewPrompt"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@id/centerGuide"
        android:layout_marginTop="50dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/buttoncolor"
        android:text="Phone"/>

</RelativeLayout>
